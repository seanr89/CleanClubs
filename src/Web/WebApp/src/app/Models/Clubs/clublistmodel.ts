/**
 * Model to support based club list details (could be removed later!)
 */
export interface ClubListModel {
    id: string;
    name: string;
    active: boolean;
    private: boolean;
    creator: string;
}
